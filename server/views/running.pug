extends ./layouts/running.pug

block content
  section.section(aria-busy='true')
    .container
      h1.sr-only Test in progress
      p.has-text-centered.mb-2
        img(src=nconf.get('html:logo:path') width=nconf.get('html:logo:width') alt=nconf.get('html:logo:alt'))
      .console-progress(role='status' aria-live='polite' aria-atomic='true')
        .spinner(aria-hidden='true')
        .label-stack
          strong Running test
          span Streaming logs · auto-refreshing
      article.message.is-success(aria-live='polite' aria-atomic='false')
        .message-header
          p#title
        .message-body#message
      noscript
        .level
          .level-item.has-text-centered
            button.button.is-primary.is-medium(onclick='window.location.href=window.location.href') #{getText('running.refresh')}
